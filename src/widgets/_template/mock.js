/**
 * Mock data for this widget.
 * Replace with BlinkX API/WebSocket calls when connecting to live data.
 *
 * Data shape should match exactly what the API will return
 * so the swap is a one-line change.
 */
export const mock = {
  // Add widget-specific mock data here
};
